env:
  LANG: "en_GB.UTF-8"

steps:

  #
  # 6.25.2
  #
  - label: 'iOS 15.0'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_0"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.1'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_1"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.2'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_2"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true


  - label: 'iOS 15.3'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_3"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true


  - label: 'iOS 15.4'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_4"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true


  - label: 'iOS 15.5'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_5"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.6'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.25.2.ipa"
          - "--farm=bb"
          - "--device=IOS_15_6"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  #
  # 6.26.0
  #
  - label: 'iOS 15.0'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_0"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.1'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_1"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.2'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_2"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.3'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_3"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.4'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_4"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.5'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_5"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true

  - label: 'iOS 15.6'
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/fixture-6.26.0.ipa"
          - "--farm=bb"
          - "--device=IOS_15_6"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/crashprobe.feature:6"
    retry:
      manual:
        permit_on_passed: true
