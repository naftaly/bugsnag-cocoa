env:
  LANG: "en_GB.UTF-8"

steps:
  ##############################################################################
  #
  # Build
  #

  - label: Build test fixtures
    key: cocoa_fixture
    timeout_in_minutes: 30
    agents:
      queue: macos-12-arm
    artifact_paths:
      - features/fixtures/ios/output/iOSTestApp.ipa
      - features/fixtures/macos/output/macOSTestApp.zip
      - features/fixtures/ios/output/ipa_url.txt
    commands:
      - bundle install
      - make test-fixtures
      - bundle exec upload-app --app=./features/fixtures/ios/output/iOSTestApp.ipa --app-id-file=./features/fixtures/ios/output/ipa_url.txt

  - label: 'BitBar Poc - iOS 15 - 15.0'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone 13 A2633 15.0.1"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true

  - label: 'BitBar Poc - iOS 15 - 15.1'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone 13 A2482 15.1 -US"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true

  - label: 'BitBar Poc - iOS 15 - 15.2'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone 13 Mini A2481 15.2 -US"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true

  - label: 'BitBar Poc - iOS 15 - 15.3'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone 13 Mini A2481 15.3 -US"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true

  - label: 'BitBar Poc - iOS 15 - 15.4'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone SE 2020 A2275 15.4.1 -US"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true

  - label: 'BitBar Poc - iOS 15 - 15.5'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone 13 Pro Max A2643 15.5"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true

  - label: 'BitBar Poc - iOS 15 - 15.6'
    depends_on: "cocoa_fixture"
    timeout_in_minutes: 90
    agents:
      queue: opensource
    plugins:
      artifacts#v1.9.0:
        download: "features/fixtures/ios/output/iOSTestApp.ipa"
        upload: "maze_output/failed/**/*"
      docker-compose#v4.7.0:
        pull: cocoa-maze-runner-bitbar
        run: cocoa-maze-runner-bitbar
        service-ports: true
        command:
          - "--app=/app/build/iOSTestApp.ipa"
          - "--farm=bb"
          - "--device=Apple iPhone 8 Plus A1897 15.6"
          - "--no-tunnel"
          - "--aws-public-ip"
          - "features/barebone_tests.feature:7"
    retry:
      manual:
        permit_on_passed: true
